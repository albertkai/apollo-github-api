##Simple app to test Apollo with Github GraphQL API.

Before running the app, replace the `REACT_APP_GITHUB_SECRET` variable value in the `.env` file with your github secret

### Then do:
```
yarn install && yarn start
```
